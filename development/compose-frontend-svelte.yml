services:
  frontend:
    build:
      context: ../webapp/frontend/svelte
      dockerfile: Dockerfile
    image: cleaning-frontend-svelte
    container_name: cleaning-frontend
    expose:
      - "3000"
